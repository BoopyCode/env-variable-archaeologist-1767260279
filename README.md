# EnvVar Archaeologist

Digs through your environment variable mess across .env files, shell configs, and actual environment. Run: `python envvar_archaeologist.py` to see where your config values are hiding (and fighting).